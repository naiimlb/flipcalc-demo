/* =====================================================================
   config.js — LE SEUL FICHIER À REMPLIR pour brancher la sauvegarde cloud
   ---------------------------------------------------------------------
   1. Crée un projet gratuit sur https://supabase.com
   2. Exécute supabase/schema.sql dans SQL Editor
   3. Project Settings → API : copie « Project URL » et la clé « anon public »
   4. Colle-les ci-dessous, commit, pousse : le site se redéploie tout seul.

   ⚠️  Ne mets JAMAIS ici la clé « service_role » (secrète) : ce fichier est
   téléchargé par chaque navigateur. La clé « anon » est publique par nature ;
   ce sont les règles Row Level Security qui protègent les données.
   ===================================================================== */

export const SUPABASE_URL = '';       // ex. 'https://abcdefghijklmno.supabase.co'
export const SUPABASE_ANON_KEY = '';  // ex. 'eyJhbGciOiJIUzI1NiIsInR5cCI6...'
